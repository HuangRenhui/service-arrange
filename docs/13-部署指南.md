# 部署指南

本文说明服务编排平台的完整部署流程：前置依赖、构建、中间件准备、后端启动、前端托管与容器化建议。

> 本地快速验证请直接看 [快速开始](11-快速开始.md)。本文面向**正式环境部署**。

---

## 📋 文档信息

| 项目 | 内容 |
| --- | --- |
| 文档名称 | 部署指南 |
| 文档版本 | v1.0 |
| 最后更新 | 2026-09-14 |
| 状态 | 现行 |
| 部署形态 | 单体双模块 + 外部中间件 |

---

## 目录

- [§1 部署拓扑](#§1-部署拓扑)
- [§2 前置准备](#§2-前置准备)
- [§3 构建产物](#§3-构建产物)
- [§4 部署中间件](#§4-部署中间件)
- [§5 部署后端](#§5-部署后端)
- [§6 部署前端](#§6-部署前端)
- [§7 容器化部署](#§7-容器化部署)
- [§8 部署后校验](#§8-部署后校验)
- [§9 横向扩容演进路线（单实例多任务 → 多实例多任务）](#§9-横向扩容演进路线单实例多任务--多实例多任务)

---

## §1 部署拓扑

### 1.1 最小部署（单机）

```
┌────────────────────────────────────────────┐
│  单台服务器                                │
│  ┌──────────┐  ┌──────────┐               │
│  │ Nginx    │  │ 后端     │               │
│  │ :80      │─▶│ :8082    │               │
│  └────┬─────┘  └────┬─────┘               │
│       │             │                     │
│       │    ┌────────┼────────┐            │
│       │    ▼        ▼        ▼            │
│       │  Mongo   RabbitMQ   ZK            │
│       │  :27017  :5672      :2181         │
│  └────┴──────────────────────────┘        │
└────────────────────────────────────────────┘
```

适用：演示、测试、小规模内部使用。

### 1.2 高可用部署

```
              ┌──────────────┐
              │  Nginx / LB  │
              └───────┬──────┘
                      │
        ┌─────────────┴─────────────┐
        ▼                           ▼
┌───────────────┐          ┌───────────────┐
│ 前端静态资源   │          │ 后端实例 × N  │
│ (Nginx 托管)   │          │ :8082         │
└───────────────┘          └───────┬───────┘
                                   │
              ┌────────────────────┼────────────────────┐
              ▼                    ▼                    ▼
      ┌──────────────┐    ┌──────────────┐    ┌──────────────┐
      │ MongoDB 副本集│    │ RabbitMQ 集群 │    │ ZK 集群(3节点)│
      └──────────────┘    └──────────────┘    └──────────────┘
```

> ⚠️ **多后端实例的硬前提**：`TaskProductor.sendTaskRun` / `sendTaskResult` 当前是**本地方法直调**而非 MQ 投递。多实例部署前必须先改造为真正投递，否则任务无法跨节点流转。详见 [项目现状](10-项目现状.md) 缺陷 P2。
>
> 📌 从「单实例多任务」演进到「多实例多任务」的完整改造清单、参数调优对照表与验证项，见本文档 [§9 横向扩容演进路线](#§9-横向扩容演进路线单实例多任务--多实例多任务)。

---

## §2 前置准备

### 2.1 软件依赖

| 依赖 | 最低版本 | 用途 | 备注 |
| --- | --- | --- | --- |
| JDK | 8 | 运行后端 | 与编译版本一致 |
| MongoDB | 4.0 | 数据存储 | 生产建议副本集 |
| RabbitMQ | 3.7 | 消息中间件 | 生产建议集群 + 镜像队列 |
| ZooKeeper | 3.4 | 分布式锁 | 生产建议 3 节点 |
| Nginx | 1.18 | 前端托管 + 反向代理 | 可选 |
| Docker | 20.10 | 容器化 | 可选 |

### 2.2 网络与端口

| 端口 | 组件 | 是否对公网开放 |
| --- | --- | --- |
| `80` / `443` | Nginx | ✅ 是 |
| `8082` | 后端 | ❌ 仅内网 |
| `27017` | MongoDB | ❌ 仅内网 |
| `5672` | RabbitMQ | ❌ 仅内网 |
| `15672` | RabbitMQ 管理台 | ❌ 仅内网/跳板 |
| `2181` | ZooKeeper | ❌ 仅内网 |

### 2.3 资源规划（参考）

| 组件 | CPU | 内存 | 磁盘 |
| --- | --- | --- | --- |
| 后端 | 2 核 | 2 GB | 10 GB |
| MongoDB | 4 核 | 8 GB | 按实例量评估，建议 SSD |
| RabbitMQ | 2 核 | 4 GB | 20 GB |
| ZooKeeper | 2 核 | 2 GB | 10 GB |

> MongoDB 容量估算：单实例文档内含完整 DSL 与关系图，约占几十 KB；按日运行量 × 保留天数估算，并预留索引空间。

---

## §3 构建产物

### 3.1 全量构建

```bash
git clone <仓库地址> service-arrange
cd service-arrange/backend
mvn clean package -DskipTests
```

产物位置：

| 模块 | 产物 |
| --- | --- |
| 根工程 / backend | 无独立产物 |
| `backend/engine` | `backend/engine/target/*.jar`（库包）|
| `backend/web` | `backend/web/target/service-arrange.jar`（可执行 Spring Boot jar）|

### 3.2 只构建 Web 模块

```bash
cd backend
mvn -pl web -am clean package -DskipTests
```

`-am` 表示同时构建其依赖模块（`engine`）。

### 3.3 前端无需构建

`frontend/` 是纯静态目录，**没有构建步骤**，直接拷贝即可部署。

---

## §4 部署中间件

### 4.1 MongoDB

```bash
# 单机
mongod --dbpath /data/mongodb --port 27017 --bind_ip 0.0.0.0

# 副本集（生产建议）
mongod --replSet rs0 --dbpath /data/mongodb --port 27017 --bind_ip 0.0.0.0
mongo --eval 'rs.initiate()'
```

> 注意：`application.yml` 中 `database` 配置为 **`admin`**（系统库）。建议生产改为独立业务库 `servicearrange`，见 [配置说明](12-配置说明.md) §6。

### 4.2 RabbitMQ

```bash
rabbitmq-server -detached

# 启用管理插件
rabbitmq-plugins enable rabbitmq_management

# 创建专用账号（不要用 guest，guest 仅允许本机登录）
rabbitmqctl add_user app '<强密码>'
rabbitmqctl set_permissions -p / app ".*" ".*" ".*"
```

交换器与队列说明：

| 项 | 值 |
| --- | --- |
| 交换器 | `servicearrange-exchange-point`（由 `${spring.application.name}-exchange-point` 生成）|
| 任务队列 routing key | `/task/run` |
| 结果队列 routing key | `/task/result` |
| 生产路由键来源 | 消息头 `headers.receiveSign` |
| 消费 ACK 模式 | `manual` |

### 4.3 ZooKeeper

```bash
# 单机
zkServer.sh start

# 集群：3 节点，各自 zoo.cfg 配置
tickTime=2000
dataDir=/data/zookeeper
clientPort=2181
initLimit=10
syncLimit=5
server.1=zk1:2888:3888
server.2=zk2:2888:3888
server.3=zk3:2888:3888
```

锁路径：`/INST_LOCK_{instId}`，`CreateMode.EPHEMERAL`。

---

## §5 部署后端

### 5.1 方式一：直接运行 jar

```bash
# 1. 准备配置（推荐用环境变量覆盖，见配置说明 §6）
export MONGO_HOST=10.0.0.11
export MONGO_USERNAME=app
export MONGO_PASSWORD='***'
export MQ_ADDRESSES=10.0.0.12:5672
export MQ_USERNAME=app
export MQ_PASSWORD='***'
export ZK_URL=10.0.0.13:2181

# 2. 启动
nohup java -jar web/target/service-arrange.jar \
  --spring.profiles.active=prod \
  > /var/log/service-arrange/app.log 2>&1 &
```

### 5.2 方式二：systemd 托管（推荐）

创建 `/etc/systemd/system/service-arrange.service`：

```ini
[Unit]
Description=Service Arrange Platform
After=network.target

[Service]
Type=simple
User=appuser
WorkingDirectory=/opt/service-arrange
EnvironmentFile=/opt/service-arrange/.env
ExecStart=/usr/bin/java -Xms1g -Xmx2g -jar /opt/service-arrange/service-arrange.jar
Restart=on-failure
RestartSec=10
StandardOutput=journal
StandardError=journal

[Install]
WantedBy=multi-user.target
```

`.env` 文件（权限设为 `600`）：

```
MONGO_HOST=10.0.0.11
MONGO_USERNAME=app
MONGO_PASSWORD=***
MQ_ADDRESSES=10.0.0.12:5672
MQ_USERNAME=app
MQ_PASSWORD=***
ZK_URL=10.0.0.13:2181
```

```bash
systemctl daemon-reload
systemctl enable --now service-arrange
systemctl status service-arrange
journalctl -u service-arrange -f
```

### 5.3 方式三：Docker 运行

见 §7。

### 5.4 DSL 文件投放

后端**固定读取** classpath 下的 `hrh_http.json`（见 [项目现状](10-项目现状.md) 缺陷 P1）。投产时有两种处理：

| 方式 | 操作 |
| --- | --- |
| 打包前进资源 | 把目标 DSL 覆盖到 `backend/web/src/main/resources/hrh_http.json` 后再 `mvn package` |
| 启动参数外置 | 若后续改造为可配置路径，用 `--servea.dsl-path=/data/dsl/main.json` 指定 |

> 当前代码**未支持**第二种方式，需先完成 `Plan` 实体改造。

---

## §6 部署前端

### 6.1 方式一：Nginx 托管（推荐）

```nginx
server {
    listen 80;
    server_name arrange.example.com;

    # 前端静态资源
    root /opt/service-arrange/frontend;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    # 反向代理后端，解决跨域
    location /servicearrange/ {
        proxy_pass http://127.0.0.1:8082/servicearrange/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 300s;
    }
}
```

> 采用此方案后，前端「接口设置」可**留空**（同源），请求 `/servicearrange/...` 会被 Nginx 转发到后端。同时后端能收到真实的请求头（`#header_$Xxx` 取值依赖此项）。

部署命令：

```bash
cp -r frontend/* /opt/service-arrange/frontend/
nginx -t && nginx -s reload
```

### 6.2 方式二：由后端托管（最简）

把 `frontend/` 下所有文件拷入后端资源目录：

```bash
cp -r frontend/* backend/web/src/main/resources/static/
```

目录结构：

```
static/
├── index.html
├── css/style.css
└── js/*.js
```

重新打包并启动后，访问 `http://{host}:8082/servicearrange/index.html`。

> 优点：无需 Nginx、无跨域问题。缺点：前端更新需重新打包后端。

### 6.3 方式三：独立静态服务

```bash
cd frontend
npx serve . -l 5173
```

在页面右上角「接口设置」填入后端完整地址（含上下文路径）。

---

## §7 容器化部署

### 7.1 后端 Dockerfile

```dockerfile
FROM maven:3.8-openjdk-8 AS builder
WORKDIR /build
COPY pom.xml .
COPY engine/pom.xml engine/
COPY web/pom.xml web/
RUN mvn dependency:go-offline -B
COPY . .
RUN mvn clean package -DskipTests

FROM openjdk:8-jre-slim
WORKDIR /app
COPY --from=builder /build/backend/web/target/*.jar app.jar

ENV TZ=Asia/Shanghai
ENV JAVA_OPTS="-Xms1g -Xmx2g"

EXPOSE 8082
ENTRYPOINT ["sh", "-c", "java $JAVA_OPTS -jar app.jar"]
```

### 7.2 前端 Dockerfile

```dockerfile
FROM nginx:1.24-alpine
COPY frontend/ /usr/share/nginx/html/
COPY nginx.conf /etc/nginx/conf.d/default.conf
EXPOSE 80
```

### 7.3 docker-compose（一体化）

```yaml
version: '3.8'
services:
  mongodb:
    image: mongo:4.4
    environment:
      MONGO_INITDB_ROOT_USERNAME: admin
      MONGO_INITDB_ROOT_PASSWORD: ${MONGO_PASSWORD}
    volumes:
      - mongo_data:/data/db
    ports:
      - "27017:27017"

  rabbitmq:
    image: rabbitmq:3.11-management
    environment:
      RABBITMQ_DEFAULT_USER: app
      RABBITMQ_DEFAULT_PASS: ${MQ_PASSWORD}
    ports:
      - "5672:5672"
      - "15672:15672"

  zookeeper:
    image: zookeeper:3.8
    environment:
      ZOO_4LW_COMMANDS_WHITELIST: "*"
    ports:
      - "2181:2181"

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    environment:
      MONGO_HOST: mongodb
      MONGO_USERNAME: admin
      MONGO_PASSWORD: ${MONGO_PASSWORD}
      MQ_ADDRESSES: rabbitmq:5672
      MQ_USERNAME: app
      MQ_PASSWORD: ${MQ_PASSWORD}
      ZK_URL: zookeeper:2181
    ports:
      - "8082:8082"
    depends_on:
      - mongodb
      - rabbitmq
      - zookeeper

  frontend:
    build:
      context: .
      dockerfile: frontend/Dockerfile
    ports:
      - "80:80"
    depends_on:
      - backend

volumes:
  mongo_data:
```

启动：

```bash
echo "MONGO_PASSWORD=***" > .env
echo "MQ_PASSWORD=***" >> .env
docker-compose up -d
docker-compose logs -f backend
```

### 7.4 K8s 部署要点

| 项 | 说明 |
| --- | --- |
| 无状态化 | 后端本身无状态，锁在 ZK，可自由扩缩容 |
| 副本数 | **必须先完成 MQ 投递改造**，否则副本数只能为 1 |
| 探针 | 当前无 actuator，建议先引入 `/actuator/health` 再做 liveness/readiness |
| 配置 | 用 ConfigMap 放非敏感项，Secret 放密码 |
| 优雅停机 | 设置 `preStop` 钩子，等待在途任务完成 |

---

## §8 部署后校验

### 8.1 逐项检查

| 序号 | 检查项 | 方法 | 期望 |
| --- | --- | --- | --- |
| 1 | 后端进程 | `ps -ef \| grep java` 或 `systemctl status` | 运行中 |
| 2 | 端口监听 | `netstat -ano \| grep 8082` | LISTENING |
| 3 | 启动日志 | `tail -f app.log` | 见 `Started ServiceArrangeApplication`，无异常堆栈 |
| 4 | Mongo 连通 | 接口调用后查 `SERVEA_INST` | 有新文档产生 |
| 5 | MQ 连通 | RabbitMQ 管理台查队列 | `servicearrange-exchange-point` 交换器存在 |
| 6 | ZK 连通 | `zkCli.sh -server host:2181` → `ls /` | 可连接，无 Curator 告警 |
| 7 | 接口可达 | `curl -X POST ".../registerOperator/save" -H "Content-Type: application/json" -d '{...}'` | 返回 `success` |
| 8 | 前端加载 | 访问 `http://host/index.html` | 页面正常渲染 |
| 9 | 前端联调 | 点「接口设置 → 连通性检测」 | 顶栏显示「已连通」 |
| 10 | 编排闭环 | 加载示例 1 → 校验 → 导出 DSL | 校验通过，JSON 规范 |

### 8.2 冒烟测试命令

```bash
# 算子查询（探针）
curl "http://localhost:8082/servicearrange/registerOperator/findById?id=1"

# 保存算子
curl -X POST "http://localhost:8082/servicearrange/registerOperator/save" \
     -H "Content-Type: application/json" \
     -d '{"type":"http","serviceName":"测试","serviceUrl":"localhost/demo","requestMethod":"get"}'

# 运行实例
curl -X POST "http://localhost:8082/servicearrange/inst/run/or/getResult/PLAN_001" \
     -F "servea_optType=run" -F "servea_sync=false"
```

### 8.3 回滚方案

| 场景 | 操作 |
| --- | --- |
| 后端异常 | 保留上一版本 jar，`systemctl stop` 后替换 jar 再 `start` |
| 前端异常 | 静态目录改名回滚（如 `frontend` ↔ `frontend.bak`），`nginx -s reload` |
| 配置错误 | 恢复 `.env` 中上一版环境变量后重启 |
| 数据异常 | 按 `instId` 清理 `SERVEA_INST` / `SERVEA_TASK` / `SERVEA_INST_LOG` 中的对应记录 |

---

## §9 横向扩容演进路线（单实例多任务 → 多实例多任务）

> **阶段一（当前）**：单后端实例、内部多任务并发执行；
> **阶段二（目标）**：多后端实例、任务按实例维度分配到不同节点执行。

### 9.1 阶段一：单实例多任务（当前形态）

单实例部署下，`@StreamListener` 的消费并发由 `task.consumer.*-concurrency` 控制（默认 `2`）。
因为 `TaskProductor.sendTaskRun` / `sendTaskResult` 目前是**本地方法直调**，消息并未真正经过 Broker，
所以**消费并发配置不会带来任何并行收益**——多任务的实际并发度取决于业务线程自身的调度，
而非消费线程数。此阶段应这样取舍：

| 调优项 | 建议 | 原因 |
| --- | --- | --- |
| `task.consumer.*-concurrency` | 保持 `1` 或 `2` 即可 | 本地直调下该参数不生效，调大只会增加空转的消费线程 |
| `task.backpressure.lock-nack-max-times` | `10`（默认）| 单实例下同一 `instId` 不会跨节点争锁，该阈值几乎不会触发 |
| `task.backpressure.requeue-backoff-base-ms` | `200`（默认）| 同上 |
| `task.http.read-timeout` | 按下游 P99 × 2 设置，默认 `30s` | **单实例下这是最关键的参数**，慢下游会占住唯一的执行链路 |
| Mongo `connections-per-host` | `50`（默认）| 单实例下足够，无需上调 |

> ⚠️ **单实例的最大瓶颈不是消费并发，而是「慢节点阻塞全流程」**。建议优先保障
> `task.http.connect-timeout` / `read-timeout` 与 Mongo `socket-timeout`，
> 让异常下游能快速失败，而不是拖住整条执行链。

### 9.2 阶段二：多实例多任务（目标形态）

#### 9.2.1 前置改造（必做，否则多实例无意义）

| 序号 | 改造项 | 说明 |
| --- | --- | --- |
| 1 | **`TaskProductor` 改为真正投递 MQ** | 当前 `sendTaskRun` / `sendTaskResult` 为本地方法直调（`TaskProductor` L58-63），必须改为经 `MqChannelProcessor` 的通道投递。这是多实例部署的**硬前提**，否则实例 A 生成的任务不会流转到实例 B |
| 2 | **投递路由键设计** | `output_channel` 已配置 `routing-key-expression: headers.receiveSign`，需确认 `receiveSign` 的取值规则，建议按「实例维度」路由，保证同一实例的消息落到同一队列 |
| 3 | **手工 ACK 语义校验** | 多实例下 `deliveryTag` 与 `Channel` 仍由各自消费者持有，现有 `ackTask` / `nAckTask` 可复用；但要确认 `channel.isOpen()` 判断在连接抖动时的行为 |
| 4 | **实例锁的语义复核** | 锁的粒度是 `instId`（`/INST_LOCK_{instId}`），多实例部署后**同一实例的结果消息仍会被串行化**，这正是设计意图；但要注意此时「抢锁失败」会变常见，退避与上限参数需要重新评估 |

#### 9.2.2 参数调优建议（多实例）

| 参数 | 单实例建议值 | 多实例建议值 | 说明 |
| --- | --- | --- | --- |
| `task.consumer.result-concurrency` | `1~2` | `4~8` | 多实例下每个节点都可独立消费，并发收益才会显现 |
| `task.consumer.run-concurrency` | `1~2` | `4~8` | 同上 |
| `task.backpressure.lock-nack-max-times` | `10` | `20~30` | 多实例争锁频率上升，上限需放宽，否则正常竞争会被误判为毒消息而丢弃 |
| `task.backpressure.requeue-backoff-base-ms` | `200` | `500~1000` | 退避基数调大，降低多实例间的锁竞争抖动 |
| `task.backpressure.requeue-backoff-max-ms` | `5000` | `10000` | 同步调大上限，避免退避被过早截断 |
| `task.backpressure.max-handle-times` | `5` | `5` | 保持不变，与部署形态无关 |
| 节点数 N | `1` | 建议 `≥ 2`，且 `N ≤ RabbitMQ 队列分区数` | 消费者数超过分区数会导致部分消费者空闲 |

#### 9.2.3 扩容时必须验证的点

- [ ] 两个实例各自运行一个流程，确认互不阻塞、结果都正确回写；
- [ ] 同一个实例的结果消息被投递到「非发起节点」，确认仍能正确接续（验证消息真正走了 MQ）；
- [ ] 刻意制造抢锁冲突（同一实例并发投递多条结果消息），确认退避生效且无消息被误丢弃；
- [ ] 停机一个实例，确认其上的结果消息能通过 requeue 被其他实例接管；
- [ ] 观察 `lockNackTimes` 是否大量增长——若持续增长说明并发数或退避参数设置不当。

### 9.3 扩容决策参考

| 场景 | 是否可直接扩到多实例 | 说明 |
| --- | --- | --- |
| 演示 / 内部试用 | ❌ 不必 | 单实例足够，扩容只增加运维成本 |
| 单实例 CPU 长期 > 80% | ✅ 可以 | 先确认 §9.2.1 改造 1 已完成 |
| 单实例任务排队积压 | ✅ 可以 | 同时上调 `consumer.*-concurrency` |
| 仅单个慢下游拖慢整体 | ⚠️ 不推荐 | 应先优化下游或用算子级超时隔离，扩容是治标 |

---

## 📚 相关文档
- 🚀 [快速开始](11-快速开始.md) — 本地开发环境启动
- ⚙️ [配置说明](12-配置说明.md) — 全部配置项与生产整改建议
- 🧪 [测试指南](14-测试指南.md) — 部署后功能验证
- 🏗️ [系统架构](15-系统架构.md) — 部署拓扑与安全设计
- 🗺️ [项目现状](10-项目现状.md) — 多实例部署的前置条件
